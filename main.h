#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdlib.h>
#include <stdarg.h>
#include <stdio.h>

int _putchar(char c);
int print_num(int n);
int print_bin(unsigned int n);
int _printf(const char *format, ...);

#endif
